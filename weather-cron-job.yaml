apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: weather-cron-job
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: weather
              image: eqkatz/weather
          restartPolicy: OnFailure